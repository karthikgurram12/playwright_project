{
  "scripts": {
    "test": "npx cucumber-js --format json:cucumber-report.json",
    "report": "ts-node support/generate-html-report.ts",
    "test:html": "npm run test || true && npm run report"
  }
}
